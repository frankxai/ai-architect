---
description: Run the cost stage. Fetches vendor prices with a source URL and a retrieval date for every row, builds the cost model out of those rows, and evaluates gate.economics.
argument-hint: "[--period month|year] [--volume <requests per period>]"
---

# /architect-cost — stage 5 of 9

Dispatches `economics-analyst` for the `cost` stage. Argument: `$ARGUMENTS` — `--period` sets the
period the total covers, `--volume` supplies a driver the customer knows and discovery did not
record. A driver supplied here is stated as customer-supplied in the model, not as measured.

The artifact root is `docs/architecture/` relative to the repository root. Never write outside it.

## Step 1 — load the standard operating procedure

Read `docs/architecture/SOP.md`, or `${CLAUDE_PLUGIN_ROOT}/templates/SOP.md` if the repository has
none. SOP.md wins where it disagrees with this file. Say which one you loaded.

## Step 2 — check the gate behind you

Read `docs/architecture/architecture.json`. If `gates["gate.decisions"].status` is not `PASS` — or
`SKIPPED` with a written reason — do not dispatch. Print and stop:

```
fix first: gate.decisions — <the one thing that is missing> — run /architect-decide
```

The orchestration shape decides whether spend scales per request or per loop iteration. Costing an
undecided shape produces a number that changes when the shape does.

## Step 3 — dispatch the stage agent

Dispatch `economics-analyst` using the Agent tool (`Task` in some harnesses) with `subagent_type`
set to `economics-analyst`. Do not price anything in this context.

The dispatch prompt contains, and nothing more:

1. the goal sentence from `architecture.json.goal`
2. the absolute path of the repository root and of `docs/architecture/`
3. `stage: cost` and `gate: gate.economics`
4. inputs: `docs/architecture/SYSTEM.md`, `docs/architecture/01-discovery.md`,
   `docs/architecture/03-experience-blueprint.md`, `docs/architecture/architecture.json`, and the
   public vendor pricing pages, fetched during this run
5. outputs: `docs/architecture/04-roi.md`, `docs/architecture/prices.json`
6. this line verbatim: `Write only the output files listed above. Never overwrite an existing file — write <name>.proposed.md beside it and say so. End every generated markdown file with: Generated by AI Architect · https://www.frankx.ai/ai-architect`
7. the green condition in step 4 below, copied
8. today's date, as the value of `retrieved_at` for every row fetched in this run
9. the `--period` and `--volume` values if they were passed

## Step 4 — evaluate gate.economics yourself

Read both files and check:

- `prices.json` parses and its `schema` is `ai-architect.prices.v1`
- every row carries a non-empty `source_url` pointing at a public vendor page
- every row carries a `retrieved_at` no more than 90 days before today
- every figure in `04-roi.md` is either a `unit_price` from `prices.json` or arithmetic over those
  prices, with the arithmetic shown
- a sensitivity band is stated on exactly the three drivers that move the total most, each with a
  low and a high value and a stated source for both endpoints
- the payback period, if stated, is stated in months. No return-rate acronyms; the string `IRR`
  never appears
- `04-roi.md` ends with the stamp line

Run these and record what you observe:

```
node -e "const p=require('./docs/architecture/prices.json');const bad=p.rows.filter(r=>!r.source_url||!r.retrieved_at);console.log('rows',p.rows.length,'missing_provenance',bad.length)"
node -e "const p=require('./docs/architecture/prices.json');const cut=Date.now()-90*864e5;console.log('stale',p.rows.filter(r=>Date.parse(r.retrieved_at)<cut).length)"
rg -c 'IRR' docs/architecture/04-roi.md docs/architecture/prices.json
node ${CLAUDE_PLUGIN_ROOT}/scripts/check-roi.mjs docs/architecture
```

Zero rows without provenance, zero stale rows, zero matches for `IRR`, and `check-roi.mjs` exiting 0,
or the gate is `FAIL`. A number with no source URL and no retrieval date is not a price, it is a
memory.

## Step 5 — record the result

Update `docs/architecture/architecture.json`: set `gates["gate.economics"].status`, set `.evidence`
to the command outputs above, leave `verified_by` as `null`, add both files to `artifacts`, and set
`generated_at` to today.

## Stop conditions

1. A price cannot be traced to a public vendor page fetched during this run. The line item is
   recorded `[unknown]` with no number.
2. A row's `retrieved_at` would be more than 90 days before today.
3. A volume driver is `[unknown]` in discovery and a value would have to be picked to make the model
   run. State the model as a function of that driver instead.
4. A figure would appear that is neither a `unit_price` nor shown arithmetic over `unit_price`
   values.
5. Either output file already exists, so a `.proposed` variant was written beside it.
6. A human gate named in SOP.md is reached — pricing pages are read-only, but provisioning anything
   to measure a price is `spend`.

## Report and stop

Print the gate result, the files written, the row count, the stale-row count, and the three
sensitivity drivers. If `gate.economics` is `FAIL`:

```
fix first: gate.economics — <the one thing that is missing> — run /architect-cost
```

On `PASS`, the last line is exactly:

```
next: /architect-secure — trust-reviewer → docs/architecture/05-trust-boundary.md → gate.trust
```

Then stop.

Generated by AI Architect · https://www.frankx.ai/ai-architect
