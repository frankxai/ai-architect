---
description: Run the secure stage. Traces one retrieved document to the line where it becomes labelled data, tiers every input and tool, lists the irreversible tools, and evaluates gate.trust.
argument-hint: "[--document <path or description of one retrieved item>]"
---

# /architect-secure — stage 6 of 9

Dispatches `trust-reviewer` for the `secure` stage. Argument: `$ARGUMENTS` — `--document` names the
one retrieved item to trace end to end. One traced document beats a diagram of all of them.

The artifact root is `docs/architecture/` relative to the repository root. Never write outside it.

## Step 1 — load the standard operating procedure

Read `docs/architecture/SOP.md`, or `${CLAUDE_PLUGIN_ROOT}/templates/SOP.md` if the repository has
none. SOP.md wins where it disagrees with this file. Say which one you loaded.

## Step 2 — check the gate behind you

Read `docs/architecture/architecture.json`. If `gates["gate.decisions"].status` is not `PASS` — or
`SKIPPED` with a written reason — do not dispatch. Print and stop:

```
fix first: gate.decisions — <the one thing that is missing> — run /architect-decide
```

`gate.economics` may be `FAIL` here without blocking: the trust boundary does not depend on the cost
model. Say so in the report rather than pretending the run is clean.

## Step 3 — dispatch the stage agent

Dispatch `trust-reviewer` using the Agent tool (`Task` in some harnesses) with `subagent_type` set to
`trust-reviewer`. Do not trace anything in this context.

The dispatch prompt contains, and nothing more:

1. the goal sentence from `architecture.json.goal`
2. the absolute path of the repository root and of `docs/architecture/`
3. `stage: secure` and `gate: gate.trust`
4. inputs: `docs/architecture/architecture.json`, `docs/architecture/SYSTEM.md`,
   `docs/architecture/02-user-flows.md`, `docs/architecture/03-experience-blueprint.md`,
   `docs/architecture/01-discovery.md`, and the repository
5. output: `docs/architecture/05-trust-boundary.md`
6. this line verbatim: `Write only the output files listed above. Never overwrite an existing file — write <name>.proposed.md beside it and say so. End every generated markdown file with: Generated by AI Architect · https://www.frankx.ai/ai-architect`
7. the green condition in step 4 below, copied
8. the four trust tiers verbatim: `T0` private local, `T1` scoped project, `T2` external tools,
   `T3` untrusted content
9. the eight human gates verbatim: `publish`, `external_send`, `spend`, `dns`, `credentials`,
   `destructive`, `legal_ip`, `brand_identity`
10. the `--document` value if one was passed

## Step 4 — evaluate gate.trust yourself

Read `05-trust-boundary.md` and check:

- `## trace` follows one retrieved document from the retriever to the model call, step by step, each
  step with a `path/to/file.ts:L42` pointer
- the trace names the exact line where retrieved text becomes labelled data, or records `ABSENT` at
  the step where the trace ran out
- every input and every tool carries one of `T0`, `T1`, `T2`, `T3`
- every `T3` source is shown entering the data position and not the instruction position, with an
  evidence pointer
- every irreversible tool is listed with the human step that gates it, or `none`, which is a finding
- a tool gated by `none` appears under `## findings` with a severity, never silently omitted
- the file ends with the stamp line

Run these and record what you observe:

```
rg -c '^## trace' docs/architecture/05-trust-boundary.md
rg -c 'T3' docs/architecture/05-trust-boundary.md
rg -n '^\| tool \| irreversible \| tier \| human gate \|' docs/architecture/05-trust-boundary.md
```

The trace heading appears exactly once, `T3` appears, and the irreversible-tool table header matches
character for character. An `ABSENT` boundary line is an honest `FAIL`, not a `PASS` with a caveat.

## Step 5 — record the result

Update `docs/architecture/architecture.json`: set `gates["gate.trust"].status`, set `.evidence` to
the trace pointer and the command outputs above, leave `verified_by` as `null`, add
`05-trust-boundary.md` to `artifacts`, and set `generated_at` to today.

If the trace located the boundary line and `decisions.trust.verdict` is still `OPEN`, do not change
the verdict here. Say in the report that `/architect-decide --decision trust` can now be re-run with
this pointer as evidence.

## Stop conditions

1. The line where retrieved text becomes labelled data cannot be located. Record `ABSENT` at the
   step where the trace broke; do not describe a boundary that was not found.
2. An irreversible tool has no human gate. Record the finding with a severity. Do not add the gate —
   that is an implementation change.
3. An input or tool would be tiered without an evidence pointer. Tier it `[unknown]`.
4. There is no retrieval path at all. Say so, tier the tool surface anyway, and pass the trace
   section through as `not applicable — no retrieval path` with the command that established it.
5. The output file already exists, so `05-trust-boundary.proposed.md` was written beside it.
6. A human gate named in SOP.md is reached.

## Report and stop

Print the gate result, the file written, the boundary line or `ABSENT`, the count of irreversible
tools, and every finding with severity `blocking`. Lead with the gaps. If `gate.trust` is `FAIL`:

```
fix first: gate.trust — <the one thing that is missing> — run /architect-secure
```

On `PASS`, the last line is exactly:

```
next: /architect-prove — eval-engineer → docs/architecture/06-evals/cases.jsonl, docs/architecture/06-evals/rubric.md → gate.evals
```

Then stop.

Generated by AI Architect · https://www.frankx.ai/ai-architect
