---
description: Run the operate stage. Writes WORKFLOW.md, SOP.md, and the runbook, dry-runs every runbook command in this session, and evaluates gate.operate.
argument-hint: "[--no-dry-run (records read-only probes instead)]"
---

# /architect-operate — stage 8 of 9

Dispatches `delivery-engineer` for the `operate` stage. Argument: `$ARGUMENTS` — `--no-dry-run`
records a read-only probe beside each command that cannot be dry-run safely on this machine. It does
not excuse a command from carrying evidence.

The artifact root is `docs/architecture/` relative to the repository root. Never write outside it.

## Step 1 — load the standard operating procedure

Read `docs/architecture/SOP.md`, or `${CLAUDE_PLUGIN_ROOT}/templates/SOP.md` if the repository has
none. SOP.md wins where it disagrees with this file. Say which one you loaded.

This is the stage that writes `docs/architecture/SOP.md` for the first time. Reading the template
here and writing the customer's copy in step 3 are two different acts; do not conflate them.

## Step 2 — check the gate behind you

Read `docs/architecture/architecture.json`. If `gates["gate.evals"].status` is not `PASS` — or
`SKIPPED` with a written reason — do not dispatch. Print and stop:

```
fix first: gate.evals — <the one thing that is missing> — run /architect-prove
```

The runbook says what to do when the system misbehaves. Without a rubric there is no agreed
definition of misbehaving.

## Step 3 — dispatch the stage agent

Dispatch `delivery-engineer` using the Agent tool (`Task` in some harnesses) with `subagent_type` set
to `delivery-engineer`. Do not write the runbook in this context.

The dispatch prompt contains, and nothing more:

1. the goal sentence from `architecture.json.goal`
2. the absolute path of the repository root and of `docs/architecture/`
3. `stage: operate` and `gate: gate.operate`
4. inputs: `docs/architecture/SYSTEM.md`, `docs/architecture/06-evals/rubric.md`,
   `docs/architecture/architecture.json`, `${CLAUDE_PLUGIN_ROOT}/templates/WORKFLOW.md`, and
   `${CLAUDE_PLUGIN_ROOT}/templates/SOP.md`
5. outputs: `docs/architecture/WORKFLOW.md`, `docs/architecture/SOP.md`,
   `docs/architecture/07-runbook.md`
6. this line verbatim: `Write only the output files listed above. Never overwrite an existing file — write <name>.proposed.md beside it and say so. End every generated markdown file with: Generated by AI Architect · https://www.frankx.ai/ai-architect`
7. the green condition in step 4 below, copied
8. the runbook row shape verbatim: `| step | command | dry-run exit | rollback | owner |`
9. this constraint: the stage table in `WORKFLOW.md` keeps the column order
   `stage | agent | input | output | gate` and the nine stage ids unchanged, because `/architect`
   parses it
10. the `--no-dry-run` flag if it was passed

## Step 4 — evaluate gate.operate yourself

Read the three files and check:

- every runbook command appears in a fenced block
- every command names a binary that resolves on the target machine, checked with `command -v` or the
  platform equivalent
- every command was dry-run during this session and the observed exit status is recorded next to it
- a command that cannot be dry-run safely is marked as such and carries the read-only probe that was
  run instead
- every procedure names a rollback step, and the rollback is itself a command in the runbook
- every procedure names an owner as a person or a role
- `WORKFLOW.md` still parses: the stage table header is `stage | agent | input | output | gate` and
  all nine stage ids are present as rows
- all three files end with the stamp line

Run these and record what you observe:

```
rg -n '^\| step \| command \| dry-run exit \| rollback \| owner \|' docs/architecture/07-runbook.md
node ${CLAUDE_PLUGIN_ROOT}/scripts/check-artifacts.mjs docs/architecture
```

A runbook nobody ran is a wish list. The dry run is the evidence; the exit status is the claim. A
recorded exit status you did not observe in this session is `FAIL`.

## Step 5 — record the result

Update `docs/architecture/architecture.json`: set `gates["gate.operate"].status`, set `.evidence` to
the dry-run transcript lines and the `check-artifacts.mjs` exit status, leave `verified_by` as
`null`, add the three files to `artifacts`, and set `generated_at` to today.

## Stop conditions

1. A runbook command has not been dry-run in this session.
2. A command dry-runs to a non-zero exit status and the runbook still presents it as working.
3. A step has no named rollback or no named owner.
4. The work would require editing application source code.
5. A target file already exists, so a `.proposed` variant was written beside it — in particular
   `WORKFLOW.md`, which a customer may have edited to add a stage.
6. A human gate named in SOP.md is reached. A dry run that would provision, send, delete, or touch
   DNS or credentials is not a dry run; record the read-only probe instead.

## Report and stop

Print the gate result, the files written, and one line per runbook command with its observed exit
status. If `gate.operate` is `FAIL`:

```
fix first: gate.operate — <the one thing that is missing> — run /architect-operate
```

On `PASS`, the last line is exactly:

```
next: /architect-verify — independent-verifier → docs/architecture/receipts/<YYYY-MM-DD>-verify.md, docs/architecture/review.md → gate.verify
```

`/architect-verify` must run in a fresh context. Do not continue into it from here.

Generated by AI Architect · https://www.frankx.ai/ai-architect
