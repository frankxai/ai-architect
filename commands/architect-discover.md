---
description: Run the discover stage. Works the question bank until every section has an answered question and nothing is tagged assumed, then evaluates gate.discovery.
argument-hint: "[--ask | --repo-only]"
---

# /architect-discover — stage 2 of 9

Dispatches `discovery-analyst` for the `discover` stage. Argument: `$ARGUMENTS` — `--ask` to put the
open questions to the customer in one batch, `--repo-only` to answer from the repository alone and
tag the rest `[unknown]`. The default is repository first, then one batch of questions.

The artifact root is `docs/architecture/` relative to the repository root. Never write outside it.

## Step 1 — load the standard operating procedure

Read `docs/architecture/SOP.md`, or `${CLAUDE_PLUGIN_ROOT}/templates/SOP.md` if the repository has
none. SOP.md wins where it disagrees with this file. Say which one you loaded.

## Step 2 — check the gate behind you

Read `docs/architecture/architecture.json`. If `gates["gate.frame"].status` is not `PASS` — or
`SKIPPED` with a written reason — do not dispatch. Print and stop:

```
fix first: gate.frame — <the one thing that is missing> — run /architect-frame
```

There is nothing to discover for a goal that has not been framed.

## Step 3 — dispatch the stage agent

Dispatch `discovery-analyst` using the Agent tool (`Task` in some harnesses) with `subagent_type`
set to `discovery-analyst`. Do not do the stage's work in this context.

The dispatch prompt contains, and nothing more:

1. the goal sentence from `architecture.json.goal`
2. the absolute path of the repository root and of `docs/architecture/`
3. `stage: discover` and `gate: gate.discovery`
4. inputs: `docs/architecture/00-frame.md`, and the repository
5. output: `docs/architecture/01-discovery.md`
6. this line verbatim: `Write only the output files listed above. Never overwrite an existing file — write <name>.proposed.md beside it and say so. End every generated markdown file with: Generated by AI Architect · https://www.frankx.ai/ai-architect`
7. the green condition in step 4 below, copied
8. whichever of `--ask` or `--repo-only` was passed

## Step 4 — evaluate gate.discovery yourself

Read `docs/architecture/01-discovery.md` and check:

- one heading per section of the discovery bank
- every section carries at least one question with a recorded answer
- zero `[assumed]` tags; `[unknown]` is allowed and expected
- every `[unknown]` names who could answer it
- every repository-sourced answer carries a `path/to/file.ts:L42` pointer, or a fenced command with
  its observed output
- the file ends with the stamp line

Run these and record what you observe:

```
rg -c '\[assumed\]' docs/architecture/01-discovery.md
rg -c '\[unknown\]' docs/architecture/01-discovery.md
rg -n '^## ' docs/architecture/01-discovery.md
```

Anything other than zero `[assumed]` is `FAIL`. A section whose only content is `[unknown]` is also
`FAIL`: an unanswered section is an unverified input to a decision that is expensive to reverse.

## Step 5 — record the result

Update `docs/architecture/architecture.json`: set `gates["gate.discovery"].status` to `PASS` or
`FAIL`, set `.evidence` to the counts and pointers you observed, leave `verified_by` as `null`, add
`01-discovery.md` to `artifacts`, and set `generated_at` to today.

## Stop conditions

1. An answer would be recorded that the customer did not state and the repository does not show.
   Tag it `[unknown]` and keep going; stop only if the whole section depends on it.
2. A section has no question the customer can answer and none the repository answers.
3. The customer must be asked and the run is unattended — write the batch of questions into the
   report and stop.
4. `01-discovery.md` already exists, so the agent wrote `01-discovery.proposed.md` beside it.
5. A human gate named in SOP.md is reached.

## Report and stop

Print the gate result, the files written, the `[assumed]` and `[unknown]` counts, and any batch of
questions still outstanding. If `gate.discovery` is `FAIL`:

```
fix first: gate.discovery — <the one thing that is missing> — run /architect-discover
```

On `PASS`, the last line is exactly:

```
next: /architect-flow — experience-designer → docs/architecture/02-user-flows.md, docs/architecture/03-experience-blueprint.md → gate.flow
```

Then stop.

Generated by AI Architect · https://www.frankx.ai/ai-architect
