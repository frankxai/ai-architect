# Experience blueprint — {{system_name}}

Written by `experience-designer` at the `flow` stage, from the stage list at the end of
`02-user-flows.md`. One row per stage, in the same order, no extras.

`gate.flow` looks for the header row below character for character. Do not reword the columns, do
not add a column, do not split the table in two.

| Stage | Human | AI/Agents | Systems |
|---|---|---|---|
| {{stage_name}} | {{what_the_person_does}} | {{what_the_model_or_agent_does}} | {{systems_touched}} |
| {{stage_name}} | {{what_the_person_does}} | {{what_the_model_or_agent_does}} | {{systems_touched}} |
| {{stage_name}} | none | {{what_the_model_or_agent_does}} | {{systems_touched}} |

Column rules:

- **Human** — what the person does at this stage. If nothing, write `none`. A stage with `none` in
  this column and an irreversible action in the Systems column is a finding for the `secure` stage.
- **AI/Agents** — what the model or agent does. Capability nouns, not vendor names.
- **Systems** — the systems touched, as capability nouns: model gateway, vector store, durable
  runtime, object store, workflow engine.

## stage coverage

| check | value |
|---|---|
| stages in `02-user-flows.md` | {{flow_stage_count}} |
| rows in the table above | {{blueprint_row_count}} |
| stages in flows but not in the table | {{missing_stages}} |
| rows in the table with no matching stage | {{extra_rows}} |

The two counts match, and both difference lists are empty, or the gate is red. Later stages cost,
secure, and evaluate against this stage list; a stage missing here is a stage nobody prices, tiers,
or writes an eval for.

## where the human can interrupt

| stage | interruption | who | what they see |
|---|---|---|---|
| {{stage_name}} | {{approve_or_interrupt}} | {{person_or_role}} | {{visible_context}} |

Approval and interruption live in the experience plane or nowhere. This table is the one the
`secure` stage reads when it looks for the human step that gates an irreversible tool.

Generated by AI Architect · https://www.frankx.ai/ai-architect
