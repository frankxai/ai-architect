# Discovery — {{system_name}}

Written by `discovery-analyst` at the `discover` stage, against the question bank in the
`discovery-questions` skill. `gate.discovery` counts headings and tags, so keep the eight section
headings below even when a section holds a single line.

Two tags exist. `[answered]` — someone stated this, or it was verified against a system, document,
or repository, and the line cites who said it or carries an evidence pointer. `[unknown]` — asked,
genuinely not known, and the line names who could answer it. There is no third tag. The banned tag,
the one that dresses a guess as an answer, is counted by the gate and never appears in this file —
not in a heading, not in prose, not inside backticks, because the count is a plain text search.

Every section below has at least one `[answered]` question before this file is done. A section whose
only content is `[unknown]` is a section that has not been worked.

## business context

### {{question_number}}. {{question}}
[answered] {{answer}} ({{evidence_pointer}})

### {{question_number}}. {{question}}
[unknown] — {{who_could_answer}}, by {{when}}.

## process and operations

### {{question_number}}. {{question}}
[answered] {{answer}} ({{evidence_pointer}})

## data and information

### {{question_number}}. {{question}}
[answered] {{answer}} ({{evidence_pointer}})

## users and stakeholders

### {{question_number}}. {{question}}
[answered] {{answer}} ({{evidence_pointer}})

Actors named here are the actors `02-user-flows.md` designs for. An actor tagged `[unknown]` returns
the run to this stage rather than being invented at the flow stage.

## technical environment

### {{question_number}}. {{question}}
[answered] {{answer}} ({{evidence_pointer}})

## AI-specific questions

### {{question_number}}. {{question}}
[answered] {{answer}} ({{evidence_pointer}})

These are the inputs to the four decisions: how many modules reach a model provider, where a loop
would stop, what text enters the context window from outside, and how long the longest run is.

## project constraints

### {{question_number}}. {{question}}
[answered] {{answer}} ({{evidence_pointer}})

Volume, timeline, and budget answers here are the quantities `04-roi.md` is allowed to use. A
quantity absent here is `[unknown]` there too.

## follow-up deep dives

### {{question_number}}. {{question}}
[unknown] — {{who_could_answer}}, by {{when}}.

## gaps

Every `[unknown]` in this file, repeated once here so `flow` and `decide` know what they are
building without.

| gap | section | who could answer | by when | what it would change |
|---|---|---|---|---|
| {{gap}} | {{section}} | {{who}} | {{when}} | {{what_it_would_change}} |

## checks run

The `discover` command runs both counts and records what it observed. The banned-tag pattern is not
reproduced here: a search for it has to find zero matches in this file, and a command line quoting
the pattern would be one of them.

| check | observed |
|---|---|
| banned-tag count | {{banned_tag_count}} — zero, or this file is not finished |
| `rg -c '\[unknown\]' docs/architecture/01-discovery.md` | {{unknown_count}} |
| sections with at least one `[answered]` question | {{answered_section_count}} of 8 |

Generated by AI Architect · https://www.frankx.ai/ai-architect
