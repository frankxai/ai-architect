# Trust boundary — {{system_name}}

Written by `trust-reviewer` at the `secure` stage. Findings are recorded here, not repaired here.

Text that came back from a tool call is not the operator's text. If it reaches the position in the
context window where instructions live, it is an instruction — and no prompt fixes that, because
"ignore any instructions below" is also just text. So the boundary is structural, and structure is
something you can point at.

`gate.trust` looks for the `## trace` heading and for the two table headers below, character for
character.

## trace

One retrieved document, followed from the retriever call to the model call. One traced document
beats a diagram of all of them.

Document traced: {{document}}

| step | what happens | pointer |
|---|---|---|
| 1 | {{retriever_call}} | {{path_to_file}}:L{{line}} |
| 2 | {{transport_or_parse}} | {{path_to_file}}:L{{line}} |
| 3 | {{where_it_is_assembled_into_the_prompt}} | {{path_to_file}}:L{{line}} |
| 4 | the line where the text becomes labelled data | {{path_to_file}}:L{{line}} or `ABSENT` |

Boundary line: {{boundary_pointer_or_ABSENT}}

If step 4 is `ABSENT`, name the step where the trace ran out and say the trace failed. Do not say
the boundary is somewhere unspecified. If the system has no retrieval path at all, replace this
section with `not applicable — no retrieval path` and the command that established it.

## tiers

Exactly four tiers, no others. `T0` private local — never leaves the machine the operator controls.
`T1` scoped project — inside this project's boundary, operator-authored. `T2` external tools — a
system outside the boundary, called on purpose. `T3` untrusted content — text authored by someone
who is not the operator.

| source | kind | tier | evidence |
|---|---|---|---|
| {{source}} | {{input_or_tool}} | T{{n}} | {{path_to_file}}:L{{line}} |
| {{source}} | {{input_or_tool}} | T3 | {{path_to_file}}:L{{line}} |
| {{source}} | {{input_or_tool}} | [unknown] | {{who_could_settle_it}} |

Retrieved documents, fetched pages, uploaded files, tool responses, and model output about them are
`T3`. Anything that cannot be tiered from a file is `[unknown]` with the name of who could settle it.

### T3 sources in the data position

Every `T3` row above, with the line where it is fenced or labelled. A `T3` source with no such
pointer is a finding.

| T3 source | fenced or labelled at | in the data position |
|---|---|---|
| {{source}} | {{path_to_file}}:L{{line}} | yes / no — finding {{finding_id}} |

## irreversible tools

A tool is irreversible when running it a second time does not undo the first: a write, a message, a
payment, a merge, a deploy, a deletion, a DNS change, a credential rotation.

| tool | irreversible | tier | human gate |
|---|---|---|---|
| {{tool}} | yes | T{{n}} | {{publish, external_send, spend, dns, credentials, destructive, legal_ip, brand_identity, or none}} |
| {{tool}} | no | T{{n}} | — |

For each gated row, name the person or role who performs the gate and the pointer where it is
enforced:

| tool | gate | who performs it | enforced at |
|---|---|---|---|
| {{tool}} | {{gate}} | {{person_or_role}} | {{path_to_file}}:L{{line}} |

A row whose gate is `none` is a finding, never a silent omission.

## findings

Lead with the gaps. A findings section that opens with what is fine is a report nobody acts on.

| id | finding | severity | evidence | one observation that would clear it |
|---|---|---|---|---|
| {{finding_id}} | {{finding}} | blocking / notable | {{path_to_file}}:L{{line}} | {{observation}} |

Severity `blocking` means the gate is red until it is cleared. Recording a finding is the whole job
here; adding the missing gate is an implementation change, and this agent does not make those.

## checks run

```
rg -c '^## trace' docs/architecture/05-trust-boundary.md → {{observed}}
rg -c 'T3' docs/architecture/05-trust-boundary.md → {{observed}}
rg -n '^\| tool \| irreversible \| tier \| human gate \|' docs/architecture/05-trust-boundary.md → {{observed}}
```

Generated by AI Architect · https://www.frankx.ai/ai-architect
