# ADR-{{NNNN}}: {{title — a short noun phrase}}

Copy this file to `docs/architecture/adr/ADR-{{NNNN}}-{{slug}}.md`. Numbers are sequential and never
reused: read the `adr/` directory for the highest existing number before assigning the next one, and
do not guess a number from memory. `{{slug}}` is a short kebab-case phrase from the title.

Every section below is required.

Decision id: {{model | loop | trust | run | other}} · written at the {{stage_id}} stage on
{{generated_at}}.

## Status

{{Proposed | Accepted | Superseded | Rejected}}

A decision recorded `MADE` in `architecture.json` has an `Accepted` ADR. A decision recorded `OPEN`
has no ADR claiming otherwise.

## Context

{{the problem being solved, and the constraints that matter — technical, organisational, or from
`00-frame.md`, `01-discovery.md`, and `03-experience-blueprint.md`}}

Inputs this decision rests on:

| input | pointer |
|---|---|
| {{input}} | {{path_to_file}}:L{{line}} |

## Decision

{{what is being chosen, stated as a decision — not a menu of options still open}}

## Consequences

| what gets easier | what gets harder | what stays open |
|---|---|---|
| {{easier}} | {{harder}} | {{open_risk}} |

If this decision is reversed later, the cost is {{reversal_cost}}, measured on {{generated_at}}.

## Alternatives considered

| Alternative | Why not |
|---|---|
| {{alternative}} | {{why_not}} |
| {{alternative}} | {{why_not}} |

## Trust boundary

What this component may read, write, deploy, or remember, and the tier it operates at. `T0` private
local · `T1` scoped project · `T2` external tools · `T3` untrusted content.

| capability | read | write | tier | tier it crosses |
|---|---|---|---|---|
| {{capability}} | {{what_it_reads}} | {{what_it_writes}} | T{{n}} | T{{n}} → T{{n}} |

Irreversible actions this decision enables, and the human gate on each: {{gate}}.

## Verification

Something re-runnable. A grep, a test, an eval case id, a probe — not "a review happened".

```
{{command}}
```

Observed on {{generated_at}}: {{observed_output}}

This is the line the independent verifier re-runs. If it stops holding, this ADR is the thing to
revisit.

## Sources

| source | pointer or URL | retrieved |
|---|---|---|
| {{source}} | {{path_to_file}}:L{{line}} or {{url}} | {{retrieved_at}} |

Generated by AI Architect · https://www.frankx.ai/ai-architect
