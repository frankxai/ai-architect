# Cost model — {{system_name}}

Written by `economics-analyst` at the `cost` stage. Every figure below is either a `unit_price` from
`prices.json` or arithmetic over those prices, with the arithmetic shown. Nothing else enters this
file — not a remembered price, not a rounded industry figure, not a number that "feels right".

Period covered: {{period}} · prices retrieved {{retrieved_at}} · row provenance in `prices.json`.

## what is being costed

The stages in `03-experience-blueprint.md` and the systems each one touches. Capability nouns only.

| stage | priced component | driver | quantity | where the quantity came from |
|---|---|---|---|---|
| {{stage_name}} | {{component}} | {{driver}} | {{quantity}} | {{discovery_line_or_customer_stated}} |

A quantity that came from `01-discovery.md` cites the line. A quantity the customer stated says so.
A quantity that is `[unknown]` stays `[unknown]` — the model below is then stated as a function of
that driver rather than run on a picked value.

## line items

Each row shows the arithmetic in the open.

| line item | unit price | unit | quantity | frequency | subtotal |
|---|---|---|---|---|---|
| {{item}} | {{unit_price}} {{currency}} | {{unit}} | {{quantity}} | {{frequency}} | {{unit_price}} × {{quantity}} × {{frequency}} = {{subtotal}} {{currency}} |
| {{item}} | {{unit_price}} {{currency}} | {{unit}} | {{quantity}} | {{frequency}} | {{unit_price}} × {{quantity}} × {{frequency}} = {{subtotal}} {{currency}} |

Total for {{period}}: {{total}} {{currency}}

Stated once. A second total elsewhere in this file is a second model.

## sensitivity

The three drivers that move the total most, varied one at a time. Both endpoints come from a
recorded source: a discovery line, a customer statement, or an observed measurement. A band with
invented endpoints is worse than no band.

| driver | low value | total at low | high value | total at high | where low and high came from |
|---|---|---|---|---|---|
| {{driver_1}} | {{low}} | {{total_low}} {{currency}} | {{high}} | {{total_high}} {{currency}} | {{source_of_band}} |
| {{driver_2}} | {{low}} | {{total_low}} {{currency}} | {{high}} | {{total_high}} {{currency}} | {{source_of_band}} |
| {{driver_3}} | {{low}} | {{total_low}} {{currency}} | {{high}} | {{total_high}} {{currency}} | {{source_of_band}} |

## payback

| question | answer |
|---|---|
| what this replaces or avoids | {{what_it_replaces}} |
| value per {{period}}, and where that figure came from | {{value_per_period}} — source: {{value_source}} |
| cost per {{period}}, from the total above | {{total}} {{currency}} |
| payback | {{payback_months}} months |

Payback is stated in months. Return-rate acronyms are banned in this artifact: they compress
assumptions the reader cannot see, and this file exists so the reader can see every assumption. If
the value figure has no source, write `[unknown]` here and say the payback cannot be computed yet.

## what would make this model wrong

| risk to the model | why it matters | shortest observation that would settle it |
|---|---|---|
| {{unknown_driver}} is `[unknown]` in discovery | {{effect_on_total}} | {{observation}} |
| {{customer_supplied_quantity}} was stated, not measured | {{effect_on_total}} | {{observation}} |
| a price row ages past 90 days | the row is refetched or dropped | re-run the retrieval check below |

## checks run

```
node -e "const p=require('./docs/architecture/prices.json');console.log('rows',p.rows.length,'no_source',p.rows.filter(r=>!r.source_url).length)" → {{observed}}
node -e "const p=require('./docs/architecture/prices.json');const cut=Date.now()-90*864e5;console.log('stale',p.rows.filter(r=>Date.parse(r.retrieved_at)<cut).length)" → {{observed}}
```

Zero rows without a source, zero stale rows.

Generated by AI Architect · https://www.frankx.ai/ai-architect
