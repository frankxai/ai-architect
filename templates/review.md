# Architecture review — {{system_name}}

Written by `independent-verifier` at the `verify` stage, in a fresh context, beside the dated
receipt in `receipts/`. It reports what the re-derivation found. It repairs nothing.

Reviewed on {{generated_at}} · receipt: `receipts/{{generated_at}}-verify.md` · evidence basis:
{{grep_or_testimony}}.

## Unowned planes

Lead with what is unowned, not with what is fine.

| plane | owner | next incident likely surfaces as |
|---|---|---|
| {{plane_id}} | unowned | {{symptom}} |

If every plane has an owner, write `none — every plane has a named owner` and keep the heading. If
the review ran without codebase access, write `Not assessed — interview mode, no codebase access.`

## The four decisions

| # | decision | verdict | evidence as re-derived |
|---|---|---|---|
| 1 | model call seam | {{MADE_or_OPEN}} | {{n}} call sites in {{files}} |
| 2 | orchestration shape | {{shape}} | exit condition at {{path_to_file}}:L{{line}} |
| 3 | trust boundary | {{MADE_or_ABSENT}} | {{path_to_file}}:L{{line}}, or `not found` |
| 4 | long-run home | {{MADE_or_OPEN}} | longest run {{x}} minutes against a ceiling of {{y}} minutes |

Every cell in the evidence column was re-derived in this context. A cell that could not be
re-derived says so, and the corresponding row in the receipt is `FAILED`.

## Failed pointers

| id | claimed | observed | recorded against |
|---|---|---|---|
| {{pointer_id}} | {{claim}} | {{observation}} | {{authoring_agent}} |

A pointer that no longer resolves is a failure recorded against the agent that wrote it, and the run
returns to that stage. It is not fixed here.

## Gate state as found

| gate | status | verified_by | note |
|---|---|---|---|
| gate.frame | {{status}} | {{verified_by}} | {{note}} |
| gate.discovery | {{status}} | {{verified_by}} | {{note}} |
| gate.flow | {{status}} | {{verified_by}} | {{note}} |
| gate.decisions | {{status}} | {{verified_by}} | {{note}} |
| gate.economics | {{status}} | {{verified_by}} | {{note}} |
| gate.trust | {{status}} | {{verified_by}} | {{note}} |
| gate.evals | {{status}} | {{verified_by}} | {{note}} |
| gate.operate | {{status}} | {{verified_by}} | {{note}} |
| gate.verify | {{status}} | independent-verifier | {{note}} |

A `SKIPPED` gate names its reason. An absent gate is recorded as `MISSING`, which is a different
fact from `FAIL`.

## Fix first

{{the one change with the highest reversal cost if deferred further}}

When several decisions are `OPEN`, the order is `trust`, `run`, `loop`, `model` — trust rises in
cost fastest, because it scales with the number of tools already shipped.

Run: `/architect-{{stage}}`

## Escalations

Dated lines. Nothing is escalated verbally.

| date | situation | escalated to | with |
|---|---|---|---|
| {{date}} | {{situation}} | {{who}} | {{what_they_need}} |

No score is assigned in this document. A number invites arguing with the number instead of fixing
the finding.

Generated by AI Architect · https://www.frankx.ai/ai-architect
