# User flows — {{system_name}}

Written by `experience-designer` at the `flow` stage. One flow per actor named in
`01-discovery.md`, and only the flows that carry the outcome in `00-frame.md`.

`gate.flow` counts the `### failure branch` and `### human step` headings and compares both counts
to the number of flows. Keep the four sub-headings, in this order, in every flow.

## Flow 1 — {{flow_name}}

Actor: {{actor}}

### trigger

{{what_starts_it}}, started by {{who_starts_it}}.

### happy path

1. {{step}} — {{evidence_pointer_or_proposed}}
2. {{step}} — {{evidence_pointer_or_proposed}}
3. {{step}} — {{evidence_pointer_or_proposed}}

Each step is one action. Where the repository already implements a step, cite it as
`path/to/route.ts:L18`. Where it does not, say the step is proposed.

### failure branch

Step most likely to fail: {{failing_step}}

| question | answer |
|---|---|
| what the user sees | {{user_sees}} |
| what the system does | {{system_does}} |
| how the user recovers | {{recovery}} |
| what is written down for the operator | {{operator_signal}} |

What the user sees and what the system does are two separate answers. "An error is shown" is
neither.

### human step

| question | answer |
|---|---|
| who | {{person_or_role}} |
| what they are deciding | {{decision}} |
| what they can see while deciding | {{visible_context}} |
| what happens if they do nothing | {{default_on_no_response}} |

Named as a person or a role, never as a component. A flow that contains an irreversible action and
no human step stops this stage; the approver is asked for, not invented.

## Flow 2 — {{flow_name}}

Actor: {{actor}}

### trigger

{{what_starts_it}}, started by {{who_starts_it}}.

### happy path

1. {{step}} — {{evidence_pointer_or_proposed}}

### failure branch

Step most likely to fail: {{failing_step}}

| question | answer |
|---|---|
| what the user sees | {{user_sees}} |
| what the system does | {{system_does}} |
| how the user recovers | {{recovery}} |
| what is written down for the operator | {{operator_signal}} |

### human step

| question | answer |
|---|---|
| who | {{person_or_role}} |
| what they are deciding | {{decision}} |
| what they can see while deciding | {{visible_context}} |
| what happens if they do nothing | {{default_on_no_response}} |

## stage list

Every stage name used across the flows above, deduplicated by meaning rather than by wording, in
order. This is the list `03-experience-blueprint.md` turns into rows, and the list `cost`, `secure`,
and `prove` work against.

1. {{stage_name}}
2. {{stage_name}}
3. {{stage_name}}

## checks run

```
rg -c '^### failure branch' docs/architecture/02-user-flows.md → {{failure_branch_count}}
rg -c '^### human step' docs/architecture/02-user-flows.md → {{human_step_count}}
```

Both counts equal the number of flows, or the gate is red.

Generated by AI Architect · https://www.frankx.ai/ai-architect
