# Runbook — {{system_name}}

Written by `delivery-engineer` at the `operate` stage. Every command in this file was run during the
session that wrote it, and the exit status recorded is the one that was observed. A runbook nobody
ran is a wish list.

Owner of record: {{owner}} · escalation path in `SOP.md`.

## before anything

Every binary named below resolves on the target machine. Checked with:

```
command -v {{binary}} → {{observed}}
command -v {{binary}} → {{observed}}
```

## procedures

| step | command | dry-run exit | rollback | owner |
|---|---|---|---|---|
| {{step_name}} | `{{command}}` | {{observed_exit}} | `{{rollback_command}}` | {{person_or_role}} |
| {{step_name}} | `{{command}}` | {{observed_exit}} | `{{rollback_command}}` | {{person_or_role}} |
| {{step_name}} | `{{command}}` | not dry-runnable — see probe below | `{{rollback_command}}` | {{person_or_role}} |

Every rollback in that table is itself a command in this runbook. A rollback described in prose is
not a rollback.

### {{step_name}}

What it does: {{what_it_does}} · when to run it: {{when}}

```
{{command}}
```

Observed on {{run_date}}: exit {{observed_exit}}

```
{{observed_output}}
```

Rollback:

```
{{rollback_command}}
```

### {{step_name}} — cannot be dry-run safely

Why: {{why_it_cannot_be_dry_run}} — it would {{publish, send, spend, change dns, touch credentials,
or destroy}}, which is a human gate in `SOP.md`.

Read-only probe run instead:

```
{{probe_command}}
```

Observed on {{run_date}}: exit {{observed_exit}}

```
{{observed_output}}
```

## when it misbehaves

"Misbehaving" means the rubric in `06-evals/rubric.md` no longer holds.

| symptom | first check | likely plane | procedure |
|---|---|---|---|
| {{symptom}} | `{{check_command}}` | {{plane_id}} | {{step_name}} |

The observed symptom and the real cause are usually in different planes. Check before fixing where
it hurts.

## resuming a stopped run

```
node {{plugin_root}}/scripts/check-artifacts.mjs docs/architecture → {{observed_exit}}
```

Then `/architect-status` to see which gate is red, and `/architect` to continue from it.

## checks run

```
rg -n '^\| step \| command \| dry-run exit \| rollback \| owner \|' docs/architecture/07-runbook.md → {{observed}}
```

Generated by AI Architect · https://www.frankx.ai/ai-architect
